server:
  port: 8888


spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/parisaaabdollahi/config-server.git
          clone-on-start: true
          username: parisaaabdollahi
          password:

    compatibility-verifier:
      enabled: false
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  application:
    name: config-server

management:
  endpoints:
    web:
      exposure:
        include: bus-refresh